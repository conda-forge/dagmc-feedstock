# This file was generated automatically from conda-smithy. To update this configuration,
# update the conda-forge.yml and/or the recipe/meta.yaml.
# -*- mode: yaml -*-

jobs:
- job: osx
  pool:
    vmImage: macOS-12
  strategy:
    matrix:
      osx_64_doubledowndoubledownmpimpich:
        CONFIG: osx_64_doubledowndoubledownmpimpich
        UPLOAD_PACKAGES: 'True'
      osx_64_doubledowndoubledownmpinompi:
        CONFIG: osx_64_doubledowndoubledownmpinompi
        UPLOAD_PACKAGES: 'True'
      osx_64_doubledowndoubledownmpiopenmpi:
        CONFIG: osx_64_doubledowndoubledownmpiopenmpi
        UPLOAD_PACKAGES: 'True'
      osx_64_doubledownnodoubledownmpimpich:
        CONFIG: osx_64_doubledownnodoubledownmpimpich
        UPLOAD_PACKAGES: 'True'
      osx_64_doubledownnodoubledownmpinompi:
        CONFIG: osx_64_doubledownnodoubledownmpinompi
        UPLOAD_PACKAGES: 'True'
      osx_64_doubledownnodoubledownmpiopenmpi:
        CONFIG: osx_64_doubledownnodoubledownmpiopenmpi
        UPLOAD_PACKAGES: 'True'
  timeoutInMinutes: 360
  variables: {}

  steps:
  # TODO: Fast finish on azure pipelines?
  - script: |
      export CI=azure
      export flow_run_id=azure_$(Build.BuildNumber).$(System.JobAttempt)
      export remote_url=$(Build.Repository.Uri)
      export sha=$(Build.SourceVersion)
      export OSX_FORCE_SDK_DOWNLOAD="1"
      export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
      export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
      if [[ "${BUILD_REASON:-}" == "PullRequest" ]]; then
        export IS_PR_BUILD="True"
      else
        export IS_PR_BUILD="False"
      fi
      ./.scripts/run_osx_build.sh
    displayName: Run OSX build
    env:
      BINSTAR_TOKEN: $(BINSTAR_TOKEN)
      FEEDSTOCK_TOKEN: $(FEEDSTOCK_TOKEN)
      STAGING_BINSTAR_TOKEN: $(STAGING_BINSTAR_TOKEN)